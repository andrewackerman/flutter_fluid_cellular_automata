import 'dart:ui';

import 'package:flame/palette.dart';

class Palette {
  static const white = BasicPalette.white;
  static const gridLine = PaletteEntry(Color(0x80808080));
  
  static const solidTile =PaletteEntry(Color(0xFF654321));
  static const waterLight = PaletteEntry(Color(0xFF0CA4FF));
  static const waterDark = PaletteEntry(Color(0xFF004772));
}